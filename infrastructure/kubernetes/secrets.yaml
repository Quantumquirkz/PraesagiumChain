# Example only — replace REPLACE_* placeholders before apply, or use External Secrets (see external-secrets.example.yaml).
# Never commit real credentials. Prefer Sealed Secrets / External Secrets Operator in production.
apiVersion: v1
kind: Secret
metadata:
  name: praesagium-secrets
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_WITH_POSTGRES_URL"
  REDIS_URL: "REPLACE_WITH_REDIS_URL"
  CLICKHOUSE_URL: "REPLACE_WITH_CLICKHOUSE_HTTP_URL"
